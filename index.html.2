---
layout: default
title: Blog
---
<div class="blog-hero">
  <span class="hero-tag">Blog</span>
  <h1>SEO na era da <span>Inteligência Artificial</span></h1>
  <p>Estratégias e aprendizados sobre como ser encontrado no Google e nas inteligências artificiais.</p>
</div>

<div class="filters">
  <button class="filter-btn active">Todos</button>
  <button class="filter-btn">SEO / GEO</button>
  <button class="filter-btn">Marketing de Conteúdo</button>
  <button class="filter-btn">Automação & CRM</button>
</div>

{% assign featured = site.posts.first %}
{% if featured %}
<div class="featured-wrap">
  <a href="{{ featured.url }}" class="featured-card">
    <div class="featured-img">
      <svg viewBox="0 0 24 24" fill="none" stroke="#e91e8c" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
    </div>
    <div class="featured-body">
      <span class="featured-tag">{{ featured.tags | first | default: "SEO" }}</span>
      <h2 class="featured-title">{{ featured.title }}</h2>
      <p class="featured-excerpt">{{ featured.excerpt | strip_html | truncate: 180 }}</p>
      <span class="featured-link">Ler artigo completo →</span>
    </div>
  </a>
</div>
{% endif %}

<div class="posts-wrap">
  <div class="posts-grid">
    {% for post in site.posts offset:1 %}
    <div class="post-card">
      <div class="post-card-img">
        <svg viewBox="0 0 24 24" fill="none" stroke="#e91e8c" stroke-width="1.5" xmlns="http://www.w3.org/2000/svg"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
      </div>
      <div class="post-card-body">
        <span class="post-card-tag">{{ post.tags | first | default: "SEO" }}</span>
        <div class="post-card-title"><a href="{{ post.url }}">{{ post.title }}</a></div>
        <p class="post-card-excerpt">{{ post.excerpt | strip_html | truncate: 120 }}</p>
        <div class="post-card-meta">
          <span class="post-card-date">{{ post.date | date: "%d/%m/%Y" }}</span>
          <span class="post-card-read">Ler →</span>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</div>
